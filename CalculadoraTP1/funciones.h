#include <stdio.h>
#include <stdlib.h>
#define FUNCIONES_H_INCLUDED

float suma(float a, float b); // Funcion para la suma de dos numeros
float resta(float a, float b); // Funcion para la resta de dos numeros
float multiplicar(float a, float b); // Funcion para multiplicar dos numeros
float dividir(float a, float b); // Funcion para dividir dos numeros
float factorial(float a);
